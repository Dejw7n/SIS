import { CenterFilterPipe } from './center-filter.pipe';

describe('CenterFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CenterFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
